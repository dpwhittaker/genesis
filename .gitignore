_site/
.jekyll-cache/
.jekyll-metadata
Gemfile.lock
.sass-cache/

# print-session skill artifacts
pdf/
node_modules/

# never commit secrets
*.env
.secrets/

# Local Jekyll preview server (Gemfile.local) — never committed build state
_site/
.jekyll-cache/
Gemfile.local.lock
vendor/
.bundle/
.develop-sessions.json
